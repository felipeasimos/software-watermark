10^8 keys test:
summary:
	each number from 1 to 10^8 is given to the encoder and then the
	resulting graph is given to the decoder to get the number back.
	If the decoder's result and the number have the same binary
	representation, the test pass, otherwise it fails.
start of tests:	8:03:41
end of tests:	8:34:09
